type Post {
    id: ID!
    title: String!
    category: String
}
 
type Author {
    id: ID!
    name: String!
    thumbnail: String
}
 
# The Root Query for the application
type Query {
    recentPosts(count: Int, offset: Int): [Post]!
}
